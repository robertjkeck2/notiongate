version: "3"

services:
  loconotion:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: loconotion:latest
    volumes:
      - ./public:/app/loconotion/dist
    restart: always
